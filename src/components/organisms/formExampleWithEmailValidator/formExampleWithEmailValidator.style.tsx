import styled from 'styled-components';

const SCFormExampleWithEmailValidator = styled.div`
display:flex;
flex-direction: column;
place-content: center;
align-items: center;

    .formExampleBasic_CardContainer {
        display: flex
    }
`;

export default SCFormExampleWithEmailValidator;
